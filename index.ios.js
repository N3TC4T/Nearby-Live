/**
 * Load the App component.
 *  (All the fun stuff happens in "/src/index.js")
 */

import { AppRegistry } from 'react-native';
import AppContainer from './src/';

AppRegistry.registerComponent('Nearby', () => AppContainer);
